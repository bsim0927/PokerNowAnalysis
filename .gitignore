# Dependencies
node_modules/

# Build output
dist/

# API config — contains endpoint URLs and optional credentials
config.js

# Environment variables (keep .env.example since it has no real credentials)
.env
.env.*
!.env.example

# macOS metadata
.DS_Store
._*

# Claude Code
.claude/
