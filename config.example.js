// Copy this file to config.js and fill in your values.
// config.js is gitignored — never commit it.
window.POKER_CONFIG = {
  // URL of your local API server (from PokerNowScraper)
  API: 'http://localhost:3456',
};
